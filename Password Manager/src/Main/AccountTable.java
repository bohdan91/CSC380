package Main;
import java.util.HashMap;
import java.util.Set;
import java.util.TreeSet;

/**
 * Created by Alex Voytovich on 2/21/2017
 */
public class AccountTable
{/*
    HashMap<String, Account> acountTable;

    public AccountTable()
    {
        this.acountTable = new HashMap<>();
    }

    public void put(String name, Account user)
    {
        acountTable.put(name, user);
    }

    public boolean contains(String name)
    {
        return acountTable.contatins(name);
    }

    public void remove(String name)
    {
        acountTable.remove(name);
    }

    public void clear()
    {
        acountTable.clear();
    }

    public Set alphaSet()
    {
        TreeSet<String> set = new TreeSet<>();
        for (String s : acountTable.keySet)
        {
            tree.add(s);
        }
        return set;
    }

    public Set typeSet(String type)
    {
        TreeSet<String> set = new TreeSet<>();
        for (String s : acountTable.keySet)
        {
            if (acountTable.get(s).getType().equals(type));
                set.add(s);
        }
    }
    */
}
